<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="gwambina_approval_bill_control.list">
      <field name="name">gwambina_approval_bill_control list</field>
      <field name="model">gwambina_approval_bill_control.gwambina_approval_bill_control</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </list>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="gwambina_approval_bill_control.action_window">
      <field name="name">gwambina_approval_bill_control window</field>
      <field name="res_model">gwambina_approval_bill_control.gwambina_approval_bill_control</field>
      <field name="view_mode">list,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="gwambina_approval_bill_control.action_server">
      <field name="name">gwambina_approval_bill_control server</field>
      <field name="model_id" ref="model_gwambina_approval_bill_control_gwambina_approval_bill_control"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="gwambina_approval_bill_control" id="gwambina_approval_bill_control.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="gwambina_approval_bill_control.menu_1" parent="gwambina_approval_bill_control.menu_root"/>
    <menuitem name="Menu 2" id="gwambina_approval_bill_control.menu_2" parent="gwambina_approval_bill_control.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="gwambina_approval_bill_control.menu_1_list" parent="gwambina_approval_bill_control.menu_1"
              action="gwambina_approval_bill_control.action_window"/>
    <menuitem name="Server to list" id="gwambina_approval_bill_control" parent="gwambina_approval_bill_control.menu_2"
              action="gwambina_approval_bill_control.action_server"/>
-->
  </data>
</odoo>
