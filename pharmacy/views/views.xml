<odoo>
  <data>
     <record model="ir.actions.act_window" id="action_medical_prescription_order">
        <field name="name">Prescription Order</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">medical.prescription.order</field>
        <field name="binding_view_types">form</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="basic_hms.medical_prescription_order_search_view" />
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <!-- Top menu item -->

    <menuitem id="priscription_order_action1" name="Prescription Orders" parent="point_of_sale.menu_point_root" action="action_medical_prescription_order"/>

  </data>
</odoo>
